log() {
    if [ "$1" = "--warn" ]; then
        TAG=WARN
    elif [ "$1" = "--error" ]; then
        TAG=ERROR
    else
        TAG=INFO
    fi

    echo "$(date '+%Y-%m-%d %T') [$TAG] $(basename ${BASH_SOURCE[-1]}): $*"
}

export -f log
